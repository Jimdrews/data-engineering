[project]
name = "spark-learning"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pyspark==4.1.1",
]

[project.optional-dependencies]
dev = [
    "ipykernel>=6.0.0",
    "pandas>=2.0.0",
    "pyarrow>=14.0.0",
]
